public class Anotacoes {
        // Void representa um método sem retorno. Se o método tiver um retorno de tipo
        // int, double, etc, eles estarão no lugar do 'void'
        // Todos os métodos tem parênteses.
        //O método 'nextInt()' não exige parâmetro, ou seja, não precisa que algo esteja dentro dos '()'
        //Já os métodos 'println()' exige um parâmetro, algo dentro dos '()' para ter o RETORNO;
        //O método 'Math.pow()' por exemplo também exige um parâmetro.
        public static void main (String[] args) {}
        public static double mult (double n1, double n2) { //o método fica FORA DO MAIN, por isso o psvm fecha as {}                                                 
        //'mult (double n1, double n2)' representa a ASSINATURA do método
        //'mult' representa o NOME que damos ao nosso método
        // No método, devemos sempre declarar o tipo da variável, e.g int, double, etc
        // O método PODE vir antes do main, não importa a posição
        }


    }

